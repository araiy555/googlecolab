name: Collect Market Indicators

on:
  schedule:
    - cron: '0 14 * * 1-5'  # 平日毎日 UTC 14時（日本時間23時）
  workflow_dispatch:  # 手動実行も可能

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install yfinance boto3
      
      - name: Run Market Indicators Collection
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: python collect_market_indicators.py
      
      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: market-indicators-results
          path: logs/
          retention-days: 7
